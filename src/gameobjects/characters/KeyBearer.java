package gameobjects.characters;

public class KeyBearer extends Skeleton{
    public KeyBearer(int level, int initialCoordX, int initialCoordY) {
        super(level, initialCoordX, initialCoordY);
    }
}
